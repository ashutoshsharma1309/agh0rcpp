{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile and run",
            "type": "shell",
            "command": "g++-15 -std=c++17 ${file} -o ${fileBasenameNoExtension} && ./${fileBasenameNoExtension} < ${fileDirname}/input.txt > ${fileDirname}/output.txt",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}